# Intentionally empty.
#
# Apache serves real files (llms.txt, llms-full.txt) from this directory
# directly off disk — they are generated by `scripts/generate-llms.mjs`
# before each `next build`. No rewrite rules are needed.
